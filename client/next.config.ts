import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: false, // Temporarily disable to prevent double API calls in development
  /* config options here */
};

export default nextConfig;
